@{
    ViewData["Title"] = "Report Submitted Successfully";
}

<div class="container mt-5">
    <div class="alert alert-success text-center" role="alert">
        <h2 class="mb-3">Thank you!</h2>
        <p>Your map report has been successfully submitted.</p>
    </div>
    <div class="text-center mt-4">
        <a href="@Url.Action("AddReport", "MapReport")" class="btn btn-primary">
            Submit Another Report
        </a>
        <a href="@Url.Action("Index", "Home")" class="btn btn-secondary ms-2">
            Back to Home
        </a>
    </div>
</div>